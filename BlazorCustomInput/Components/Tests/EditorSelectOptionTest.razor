@typeparam Tval

@code {
    [CascadingParameter] public EditorSelectTest<Tval>? Parent { get; set; }

    [Parameter] public Tval? Value { get; set; }

    [Parameter] public RenderFragment? ChildContent { get; set; }
    [Parameter] public bool IsPlaceholder { get; set; }
    protected override void OnInitialized()
    {
        Parent?.RegisterOption(this);
    }
}